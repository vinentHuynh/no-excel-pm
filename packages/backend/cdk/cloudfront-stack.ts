// CloudFront provisioning used to live here, but Amplify now manages
// distribution and certificates for the frontend. We intentionally leave this
// file empty to avoid reintroducing the deprecated stack while keeping the
// TypeScript compiler satisfied.

export {};
